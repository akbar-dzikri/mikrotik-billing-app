DONE: Ported storefront components and routes from ARNET Billing.

All 11 new files created:
- 3 storefront components (StorefrontHeader, PackageCard, CheckoutSheet)
- 6 storefront route pages (beli/[slug] + pay + success, voucher/[token] + pay + success)
- 2 API routes (public/voucher/[token] GET, voucher/[token]/order POST)