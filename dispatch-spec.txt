Implement the full Vitest test suite for billing-app.

Work required:

1. Install vitest, add test scripts to package.json
2. Create vitest.config.ts with @/* path alias resolution
3. Create tests/setup.ts with env vars (ROUTER_ENCRYPTION_KEY as 64-char hex)
4. Create tests/helpers/factories.ts with buildSession, buildCustomer, buildPlan, buildRouter, buildRequest
5. Create tests/helpers/mocks.ts with createMockQuery (chainable proxy) and createMockDb
6. Create tests/lib/cn.test.ts (4 test cases: conflict resolution, conditional classes, empty)
7. Create tests/lib/crypto.test.ts (3 test cases: roundtrip, IV randomization, decrypt reverses encrypt)
8. Create tests/lib/auth-helpers.test.ts (3 test cases: isSuperAdmin, routerOwnerFilter, routerOwnerWhere)
9. Create tests/app/api/customers/route.test.ts (8 test cases: GET 401, GET 200, POST 400, POST 404 router, POST 404 plan, POST 201, POST warning)
10. Create tests/app/api/plans/route.test.ts (8 test cases: GET 401, GET 200, POST 400, POST 404, POST 201, POST warning)

Mock strategy: vi.mock('@/lib/db'), vi.mock('@/lib/auth-helpers'), vi.mock('@/lib/devices/resolver'). No real DB, no network.
Out of scope: modifying source files, taste files.
